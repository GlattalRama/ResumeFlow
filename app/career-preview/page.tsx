import CareerPreview from "@/components/career/CareerPreview";

export const dynamic = "force-dynamic";

// Standalone visual prototype of the redesigned "Career Growth System".
// Mock data only — no persistence, no AI calls, no auth. For design review.
export default function CareerPreviewPage() {
  return <CareerPreview />;
}
